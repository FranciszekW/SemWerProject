The Language FraJer
BNF Converter


%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of FraJer==
===Identifiers===
Identifiers //Ident// are unquoted strings beginning with a letter,
followed by any combination of letters, digits, and the characters ``_ '``
reserved words excluded.


===Literals===
Integer literals //Integer// are nonempty sequences of digits.




===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in FraJer are the following:
  | ``Array`` | ``Bool`` | ``BoolFunc`` | ``Dict``
  | ``Int`` | ``IntFunc`` | ``and`` | ``assignment``
  | ``break`` | ``continue`` | ``debug`` | ``disable``
  | ``else`` | ``enable`` | ``false`` | ``for``
  | ``has`` | ``if`` | ``key`` | ``lambda``
  | ``none`` | ``or`` | ``outer`` | ``print``
  | ``reading`` | ``return`` | ``set`` | ``skip``
  | ``swap`` | ``to`` | ``true`` | ``while``
  | ``xor`` |  |  |

The symbols used in FraJer are the following:
  | + | - | / | *
  | % | ++ | -- | [
  | ] | ( | ) | ==
  | <= | >= | < | >
  | != | ! | , | ->
  | { | } | : | ;
  | = | += | -= | *=
  | /= | %= |  |

===Comments===
Single-line comments begin with //.Multiple-line comments are  enclosed with /* and */.

==The syntactic structure of FraJer==
Non-terminals are enclosed between < and >.
The symbols -> (production),  **|**  (union)
and **eps** (empty rule) belong to the BNF notation.
All other symbols are terminals.

  | //Type// | -> | ``Int``
  |  |  **|**  | ``Bool``
  | //FType// | -> | ``IntFunc``
  |  |  **|**  | ``BoolFunc``
  | //VarIdent// | -> | //Ident//
  | //ArrIdent// | -> | //Ident//
  | //DictIdent// | -> | //Ident//
  | //FuncIdent// | -> | //Ident//
  | //Expr// | -> | //IExpr//
  |  |  **|**  | //BExpr//
  | //IExpr// | -> | //IExpr// ``+`` //IExpr1//
  |  |  **|**  | //IExpr// ``-`` //IExpr1//
  |  |  **|**  | //IExpr1//
  | //IExpr1// | -> | //IExpr1// ``/`` //IExpr2//
  |  |  **|**  | //IExpr1// ``*`` //IExpr2//
  |  |  **|**  | //IExpr1// ``%`` //IExpr2//
  |  |  **|**  | //IExpr2//
  | //IExpr2// | -> | //Integer//
  |  |  **|**  | //VarIdent//
  |  |  **|**  | //VarIdent// ``++``
  |  |  **|**  | ``++`` //VarIdent//
  |  |  **|**  | //VarIdent// ``--``
  |  |  **|**  | ``--`` //VarIdent//
  |  |  **|**  | //ArrIdent// ``[`` //IExpr// ``]``
  |  |  **|**  | //DictIdent// ``[`` //IExpr// ``]``
  |  |  **|**  | //DictIdent// ``[`` //BExpr// ``]``
  |  |  **|**  | //FuncIdent// ``(`` //Args// ``)``
  |  |  **|**  | ``(`` //IExpr// ``)``
  | //BExpr1// | -> | ``true``
  |  |  **|**  | ``false``
  |  |  **|**  | //VarIdent//
  |  |  **|**  | //IExpr// ``==`` //IExpr//
  |  |  **|**  | //IExpr// ``<=`` //IExpr//
  |  |  **|**  | //IExpr// ``>=`` //IExpr//
  |  |  **|**  | //IExpr// ``<`` //IExpr//
  |  |  **|**  | //IExpr// ``>`` //IExpr//
  |  |  **|**  | //IExpr// ``!=`` //IExpr//
  |  |  **|**  | //BExpr// ``==`` //BExpr//
  |  |  **|**  | //BExpr// ``!=`` //BExpr//
  |  |  **|**  | ``!`` //BExpr1//
  |  |  **|**  | //BExpr// ``or`` //BExpr1//
  |  |  **|**  | //BExpr// ``and`` //BExpr1//
  |  |  **|**  | //BExpr// ``xor`` //BExpr1//
  |  |  **|**  | //ArrIdent// ``[`` //IExpr// ``]``
  |  |  **|**  | //DictIdent// ``[`` //IExpr// ``]``
  |  |  **|**  | //DictIdent// ``[`` //BExpr// ``]``
  |  |  **|**  | //DictIdent// ``has`` ``key`` ``[`` //IExpr// ``]``
  |  |  **|**  | //DictIdent// ``has`` ``key`` ``[`` //BExpr// ``]``
  |  |  **|**  | //FuncIdent// ``(`` //Args// ``)``
  |  |  **|**  | ``(`` //BExpr// ``)``
  | //BExpr// | -> | //BExpr1//
  | //Args// | -> | ``none``
  |  |  **|**  | //Expr//
  |  |  **|**  | //Expr// ``,`` //Args//
  | //Lambda// | -> | //FType// ``lambda`` ``(`` //Params// ``)`` ``->`` ``{`` //Instr// ``}``
  | //Params// | -> | ``none``
  |  |  **|**  | //VarIdent// ``:`` //Type//
  |  |  **|**  | //FuncIdent// ``:`` //FType//
  |  |  **|**  | //Lambda//
  |  |  **|**  | //VarIdent// ``:`` //Type// ``,`` //Params//
  |  |  **|**  | //FuncIdent// ``:`` //FType// ``,`` //Params//
  |  |  **|**  | //Lambda// ``,`` //Params//
  | //Instr1// | -> | ``skip``
  |  |  **|**  | ``if`` ``(`` //BExpr// ``)`` ``{`` //Instr// ``}`` ``else`` ``{`` //Instr// ``}``
  |  |  **|**  | ``if`` ``(`` //BExpr// ``)`` ``{`` //Instr// ``}``
  |  |  **|**  | ``while`` ``(`` //BExpr// ``)`` ``{`` //Instr// ``}``
  |  |  **|**  | ``for`` ``(`` //VarIdent// ``=`` //IExpr// ``to`` //IExpr// ``)`` ``{`` //Instr// ``}``
  |  |  **|**  | ``return`` ``(`` //IExpr// ``)``
  |  |  **|**  | ``print`` ``(`` //IExpr// ``)``
  |  |  **|**  | ``swap`` ``(`` //VarIdent// ``,`` //VarIdent// ``)``
  |  |  **|**  | ``break`` ``(`` //IExpr// ``)``
  |  |  **|**  | ``break``
  |  |  **|**  | ``continue`` ``outer`` ``(`` //IExpr// ``)``
  |  |  **|**  | ``continue``
  |  |  **|**  | //Type// //VarIdent// ``=`` //Expr//
  |  |  **|**  | ``Array`` //Type// //ArrIdent// ``[`` //IExpr// ``]`` ``(`` //Expr// ``)``
  |  |  **|**  | ``Array`` //Type// //ArrIdent// ``[`` //IExpr// ``]``
  |  |  **|**  | //ArrIdent// ``[`` //IExpr// ``]`` ``=`` ``(`` //IExpr// ``)``
  |  |  **|**  | ``Dict`` //Type// //DictIdent//
  |  |  **|**  | //DictIdent// ``[`` //IExpr// ``]`` ``set`` ``(`` //IExpr// ``)``
  |  |  **|**  | //FType// //FuncIdent// ``(`` //Params// ``)`` ``{`` //Instr// ``}``
  |  |  **|**  | //VarIdent// ``=`` //IExpr//
  |  |  **|**  | //VarIdent// ``+=`` //IExpr//
  |  |  **|**  | //VarIdent// ``-=`` //IExpr//
  |  |  **|**  | //VarIdent// ``*=`` //IExpr//
  |  |  **|**  | //VarIdent// ``/=`` //IExpr//
  |  |  **|**  | //VarIdent// ``%=`` //IExpr//
  |  |  **|**  | ``debug`` ``assignment`` ``enable`` //VarIdent//
  |  |  **|**  | ``debug`` ``assignment`` ``disable`` //VarIdent//
  |  |  **|**  | ``debug`` ``reading`` ``enable`` //VarIdent//
  |  |  **|**  | ``debug`` ``reading`` ``disable`` //VarIdent//
  | //Instr// | -> | //Instr1// ``;`` //Instr//
  |  |  **|**  | //Instr1//



%% File generated by the BNF Converter (bnfc 2.9.5).
